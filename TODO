* argument to tagged:
   String -> Decoder a
   List (String, Decoder a)
   Dict String (Decoder a)

* vector vs array, list to array? array to list?
* index from array or list

* symbols and /

* multiple elements?

* generates invalid EDN happily (symbols, particularly)

* upstream Parser

* floating point parsing: NaN, Inf
* big numbers
